<h2 class="text-center">Book a s***t 🚽</h2>
  <div class="form-container">
    <%= simple_form_for ([@toilet, @booking]) do |f| %>
            <div class="mb-3">
      <%= f.input :date, label: 'Start', as: :datetime, html5: true %>
            </div>
            <div class="mb-3">
      <%= f.input :duration, label: 'End', as: :datetime, html5: true %>
            </div>
      <%= f.submit 'Book a s***t', class: "btn-submit"%>
    <% end %>
  </div>
